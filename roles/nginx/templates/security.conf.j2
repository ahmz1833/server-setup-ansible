# Managed by Ansible

server_tokens {{ nginx_server_tokens }};

# Remove Server header entirely if headers-more module is loaded
{% if nginx_hide_server_header | default(true) %}
more_clear_headers Server;
{% endif %}
